version: "3.9"

services:
  pliki-api:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./texts:/app/texts
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
